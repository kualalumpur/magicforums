<div class="header">
  <!-- header page html in here -->

  <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= link_to "Old Folks Forum", root_path, class: "navbar-brand" %>
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#header-dropdown" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>

        <div class="collapse navbar-collapse" id="header-dropdown">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "About", about_path %></li>
            <li><%= link_to "Topics", topics_path %></li>
            <li><% if current_user %>
              <%= link_to "Account", edit_user_path(session) %>
            <% else %>
              <%= link_to "Register", new_user_path %>
            <% end %></li>
            <li><% if current_user %>
              <%= link_to "Logout", session_path(session), method: :delete %>
            <% else %>
              <%= link_to "Login", new_session_path %>
            <% end %></li>
          </ul>
        </div>
      </div>
    </nav>
</div>
