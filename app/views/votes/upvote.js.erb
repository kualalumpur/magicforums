$('#flash-messages-container').html("<%=j render partial: 'partials/flash_messages' %>")
// have to remove the ajax below otherwise the flash messages above wont show when action cable is on
// $('.comment[data-id="<%=@comment.id%>"] .votes').html(<%= pluralize(@comment.total_votes(@comment.id), 'vote') %>)
